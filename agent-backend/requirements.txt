# AEGIS Backend — Python dependencies
# Install with: pip install -r requirements.txt
# Tested on Python 3.14 (system); compatible with Python 3.11+

# Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Data validation
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Async HTTP client (LLM calls only — no requests library)
httpx>=0.28.0

# Retry / backoff
tenacity>=9.0.0

# Environment variable loading
python-dotenv>=1.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
